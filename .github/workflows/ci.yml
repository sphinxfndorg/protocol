# CI Workflow for Sphinx Protocol
# Runs on every push and pull request to ensure code quality
# Note: GUI packages require Fyne dependencies
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install system dependencies for Fyne/GUI
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            libegl1-mesa-dev \
            libx11-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxi-dev \
            libxxf86vm-dev \
            libxkbcommon-dev \
            pkg-config

      - name: Run go fmt
        run: go fmt ./...

      - name: Run go vet
        run: go vet ./...

      - name: Run tests with race detection
        # The default go test timeout is 10m PER PACKAGE. The SPHINCS+-heavy
        # crypto tests in src/bind legitimately need ~10m on their own under
        # -race, so raise the ceiling to leave headroom on slower runners.
        run: go test -race -v -timeout 30m ./...

      - name: Verify build
        run: go build -v ./src/cli